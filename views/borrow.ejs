<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
  <title>Walican</title>
  <link rel="stylesheet" type="text/css" href="stylesheets/style.css">
</head>
<body>
  <div class="header">
    <h1>Walican</h1>
    <p><b>貸した人を選択してください</b></p>
    <form name="sendPayInfo" action="/borrow/regist" method="POST">
      <div>
        <label>payment ID</label>
        <input type="text" name="payId" value="<%= payId %>" readonly>
      </div>
      <div>
        <label>金額</label>
        <input type="text" name="amount">
      </div>
  </div>
    <div class="user-box">
      <div>
        <% users.forEach((user) => { %>
          <% if (user.id != parent) { %>
            <div class="user_column">
              <input class="radio-input" id="<%= user.id %>" type="radio" name="target_user" value="<%= user.id %>">
              <label for="<%= user.id %>">
                <img class="user_img" src="<%= user.img %>" alt="">
                <%= user.name %>
              </label>
              <div class="clear_div"></div>
            </div>
          <% } %>
        <% }); %>
      </div>
    </form>
  </div>
  <div class="send_button">
    <p>決定</p>
    <a href="javaScript:send()"></a>
  </div>

  <script>
    function send() {
      document.sendPayInfo.submit();
    }
  </script>
</body>
</html>